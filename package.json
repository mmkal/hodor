{
  "name": "hodor",
  "version": "0.0.1",
  "description": "Hodor. http://lisperator.net/pltut/parser/",
  "main": "hodor.hodor",
  "scripts": {
    "watch": "npm test && chokidar \"**/*.ts\" \"**/*.hodor\" --command \"npm test\"",
    "build": "tsc -p .",
    "test": "npm run build && npm run _test",
    "_test": "tap js/test/**/*.js | tap-xunit"
  },
  "keywords": [
    "hodor"
  ],
  "author": "mmkal",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "glob": "^7.1.0",
    "tap-dot": "^1.0.5",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0",
    "tape-watch": "^2.2.3",
    "zopf": "^4.5.0"
  }
}
